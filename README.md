# 🍽️ RestoMaps Analytics

Автоматизированная система для сбора, анализа и визуализации данных о ресторанах и кафе с интеграцией Яндекс.Карт и Notion.

## 🎯 Возможности

- 🔄 **Автоматическая синхронизация** данных из Notion API
- 📥 **Парсинг отзывов** с Яндекс.Карт с помощью Selenium
- 🤖 **NLP обработка** текстов отзывов (извлечение тегов, анализ тональности)
- 📊 **Интерактивная аналитика** с картами, графиками и фильтрами
- 🗄️ **База данных PostgreSQL** для хранения метаданных
- 📈 **Детальное логирование** всех операций
- ⏰ **Планировщик задач** для автоматизации процессов

## 🔧 Как это работает

### Процесс обработки

Система работает в несколько этапов:

1. **Синхронизация с Notion** (`notion`)
   - Получение списка ресторанов из Notion API
   - Обновление метаданных (название, адрес, личные оценки)
   - Геокодирование адресов через Яндекс.Геокодер
   - Сохранение в базу данных PostgreSQL
   - Удаление ресторанов, которых больше нет в Notion

2. **Парсинг отзывов** (`reviews`)
   - Автоматизированный парсинг отзывов с Яндекс.Карт
   - Использование Selenium WebDriver для обхода защиты
   - Извлечение текста отзывов, рейтингов, дат
   - Сохранение в базу данных с дедупликацией
   - Обновление средних рейтингов ресторанов

3. **NLP обработка** (`nlp`)
   - Анализ текстов отзывов с помощью spaCy и NLTK
   - Автоматическое извлечение тегов и ключевых слов
- Анализ тональности отзывов
- Кластеризация похожих заведений
   - Сохранение результатов для аналитики

4. **Веб-интерфейс** (`ui`)
   - Интерактивные карты с фильтрацией
   - Детальная статистика по ресторанам
   - Анализ динамики рейтингов
   - Визуализация распределения заведений

### Управление состоянием

Система использует статусы для отслеживания прогресса:
- `link_status` - статус парсинга отзывов (PENDING → PARSED → FAILED)
- `last_parsed_at` - время последнего парсинга
- `reviews_count` - количество найденных отзывов
- `average_rating` - средний рейтинг из отзывов

### Планировщик задач

Автоматическое выполнение задач по расписанию:
- **06:00** - Синхронизация с Notion
- **08:00** - Парсинг новых отзывов
- **09:00** - NLP обработка отзывов
- **02:00** - Автоматический бэкап базы данных

## 🎛️ Команды

Система использует **Click** для создания удобного CLI интерфейса.

### Веб-интерфейс
```bash
python main.py ui                                # Запуск веб-интерфейса
python main.py                                    # По умолчанию запускает UI
```

### Синхронизация данных
```bash
python main.py notion                            # Синхронизация с Notion
python main.py reviews                           # Парсинг отзывов
```

### Управление базой данных
```bash
python main.py init-db                           # Инициализация БД
```

### Планировщик и автоматизация
```bash
python main.py scheduler                         # Запуск планировщика
python main.py full                              # Полный цикл + UI
python main.py initial                           # Первоначальный прогон всех данных
python main.py check-failed                      # Повторная проверка ресторанов с ошибками
```

### Docker команды (через Makefile)
```bash
make up                                          # Запуск сервисов
make down                                        # Остановка сервисов
make notion                                      # Синхронизация с Notion
make reviews                                     # Парсинг отзывов
make backup                                      # Бэкап БД
make logs                                        # Просмотр логов
make status                                      # Статус сервисов
```

## 📊 Статусы обработки

- `PENDING` - Ожидает парсинга отзывов
- `PARSED` - Отзывы успешно спарсены
- `FAILED` - Ошибка при парсинге
- `NOT_FOUND` - Ресторан не найден на Яндекс.Картах

## 🛠️ Используемые технологии

### Управление зависимостями и разработка
- **`Python 3.11+`** - основной язык разработки
- **`Docker & Docker Compose`** - контейнеризация и оркестрация
- **`Makefile`** - автоматизация команд разработки

### Backend & Core
- **`Streamlit`** - фреймворк для создания интерактивных веб-приложений
- **`SQLAlchemy 2.0+`** - ORM для работы с базой данных
- **`PostgreSQL 15`** - реляционная база данных
- **`APScheduler`** - планировщик задач (cron-like scheduler)

### Web Scraping & Automation
- **`Selenium 4.15+`** - автоматизация веб-браузера для парсинга
- **`BeautifulSoup4`** - парсинг HTML и XML
- **`httpx`** - современный HTTP клиент
- **`webdriver-manager`** - автоматическое управление драйверами

### Data Processing & Analysis
- **`Pandas`** - анализ и обработка данных
- **`NumPy`** - вычисления с многомерными массивами


### Visualization & Mapping
- **`Folium`** - создание интерактивных карт
- **`PyDeck`** - 3D визуализация данных
- **`Plotly`** - интерактивная визуализация данных
- **`Matplotlib`** - построение графиков и диаграмм
- **`Seaborn`** - статистическая визуализация данных

### APIs & Integrations
- **`notion-client`** - официальный клиент для Notion API
- **`Yandex.Geosuggest API`** - поиск и подсказки мест по названию
- **`Yandex.Geocoder API`** - геокодирование адресов (получение координат)
- **`Yandex.Maps`** - источник отзывов и рейтингов заведений

### Logging & Monitoring
- **`loguru`** - продвинутое логирование с ротацией файлов
- **`Sentry SDK`** - мониторинг ошибок и производительности в production
- **`python-dotenv`** - управление переменными окружения

### CLI & Development Tools
- **`Click`** - создание командной строки интерфейса (CLI)
- **`Ruff`** - быстрый линтер и форматтер кода Python
- **`pytest`** - тестирование
- **`mypy`** - статическая типизация

## 🏗️ Архитектура

Система построена на модульной архитектуре:

- **Core модуль** - основная логика приложения и планировщик
- **Database модуль** - модели данных и CRUD операции
- **Parsers модуль** - парсинг данных из внешних источников
- **UI модуль** - веб-интерфейс и компоненты визуализации
- **Jobs модуль** - автоматические задачи
- **Config модуль** - управление конфигурацией

## 📁 Структура проекта

```
├── config/                     # Конфигурация и настройки
├── core/                       # Основная логика приложения
│   ├── app.py                  # Streamlit приложение
│   ├── job_manager.py          # Менеджер задач
│   └── scheduler.py            # Планировщик
├── database/                   # Работа с базой данных
│   ├── models.py               # SQLAlchemy модели
│   ├── crud.py                 # CRUD операции
│   └── database.py             # Подключение к БД
├── parsers/                    # Парсеры данных
│   ├── notion_data.py          # Синхронизация с Notion
│   ├── ya_maps_reviews_parser.py # Парсинг отзывов
│   └── nlp_analyzer.py         # NLP обработка
├── ui/                         # Веб-интерфейс
│   ├── pages/                  # Страницы приложения
│   └── components/             # Компоненты UI
├── jobs/                       # Автоматические задачи
├── scripts/                    # Вспомогательные скрипты
├── logs/                       # Логи приложения
├── main.py                     # Точка входа
├── docker-compose.yml          # Docker конфигурация
├── Makefile                    # Команды разработки
└── requirements.txt            # Зависимости
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Клонирование репозитория
git clone <repository-url>
cd RestoMaps-Analytics

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Создайте файл `config/.env` на основе `env.example`:

```bash
cp env.example config/.env
```

Заполните необходимые переменные:
- `NOTION_API_KEY` - API ключ Notion
- `NOTION_DATABASE_ID` - ID базы данных Notion
- `YA_GEO_CODER_API_KEY` - API ключ Яндекс.Геокодера
- `POSTGRES_*` - настройки базы данных

### 3. Инициализация базы данных

```bash
python main.py init-db
```

### 4. Первоначальная синхронизация

```bash
python main.py initial
```

### 5. Запуск веб-интерфейса

```bash
python main.py ui
```

Откройте в браузере: http://localhost:8501

## 🐳 Docker запуск

### 1. Настройка

```bash
# Клонирование и настройка
git clone <repository-url>
cd RestoMaps-Analytics
cp env.example config/.env
# Отредактируйте config/.env
```

### 2. Запуск через Docker Compose

```bash
# Запуск всех сервисов
make up

# Или напрямую
docker-compose up -d
```

### 3. Первоначальная настройка

```bash
# Инициализация БД
make init-db

# Синхронизация с Notion
make notion

# Парсинг отзывов
make reviews
```

### 4. Доступ к приложению

- **Локально**: http://localhost:8501
- **В продакшене**: https://your-domain.com (через Traefik)

## 📋 Требования

### Обязательные

- **Python 3.11+** - основной язык разработки
- **PostgreSQL 15+** - база данных для хранения метаданных
- **Chrome/Chromium** - для Selenium WebDriver
- **Docker & Docker Compose** - для контейнеризации
- **API ключи**:
  - Notion API ключ
  - Яндекс.Геокодер API ключ

### Рекомендуемые (для производительности)

- **Многоядерный процессор** - для параллельной обработки
- **SSD накопитель** - для быстрого доступа к данным
- **Стабильное интернет-соединение** - для парсинга и API запросов
- **Не менее 4 ГБ RAM** - для обработки больших объемов данных
- **Traefik** - для production развертывания с SSL

### Система поддерживает

- **Асинхронные операции** - эффективная работа с I/O операциями
- **Многопоточность** - параллельная обработка данных
- **Автоматические бэкапы** - резервное копирование базы данных
- **Мониторинг ошибок** - интеграция с Sentry
- **Горизонтальное масштабирование** - через Docker Swarm или Kubernetes

## 📚 Документация

- **[docs/TECHNICAL.md](docs/TECHNICAL.md)** — технические детали, архитектура, схемы
- **[docs/DEPLOY.md](docs/DEPLOY.md)** — подробные инструкции по развертыванию

## 🔧 Управление через Makefile

```bash
# Основные команды
make up          # Запуск сервисов
make down        # Остановка сервисов
make restart     # Перезапуск сервисов
make status      # Статус сервисов

# Управление данными
make notion      # Синхронизация с Notion
make reviews     # Парсинг отзывов
make backup      # Бэкап БД

# Разработка
make lint        # Проверка кода с помощью Ruff
make format      # Форматирование кода с помощью Ruff
make check       # Линтинг + форматирование (Ruff)

# Мониторинг
make logs        # Просмотр логов
make stats       # Использование ресурсов
make shell       # Подключение к контейнеру
make db          # Подключение к PostgreSQL

# Очистка
make clean       # Очистка контейнеров и volumes
make clean-logs  # Очистка логов
```

## 🎯 Особенности системы

### Уникальные возможности

- **Интеграция личных и общедоступных данных** - объединение заметок из Notion с отзывами с Яндекс.Карт
- **Автоматизированный парсинг** - обход защиты Яндекс.Карт с помощью Selenium
- **NLP анализ** - извлечение тегов и анализ тональности отзывов
- **Интерактивная визуализация** - карты, графики и фильтры в реальном времени
- **Автоматизация процессов** - планировщик задач для регулярного обновления данных

### Конкурентные преимущества

- ✅ Полная автоматизация сбора данных
- ✅ Интеграция персональных оценок
- ✅ Современная визуализация данных
- ✅ Комплексная NLP аналитика
- ✅ Простота развертывания через Docker
- ✅ Масштабируемость и надежность